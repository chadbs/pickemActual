[phases.install]
cmds = ["npm ci --include=dev", "cd client && npm ci --include=dev", "cd server && npm ci --include=dev"]

[phases.build]
cmds = ["npm run build:all"]

[start]
cmd = "npm run start:prod"